- View the optimal layout for the component depending on their device's screen size
- Control the toggle with both their mouse/trackpad and their keyboard
- **Bonus**: Complete the challenge with just HTML and CSS


- content



tricks:
- if on hover has border, but normal no hover, then during hover, the element will move around to make space for the border


1. change content using html + css
- add separate element using html, then hide/unhide using css when user click toggle

2. add the content we want to change as css variable, then change the content when user click toggle
https://stackoverflow.com/a/71749024

3. use css psuedo element before, after
https://developer.mozilla.org/en-US/docs/Web/CSS/content

4. change content using html + css
- add separate element using html, then use before, after to change data content
